"use strict";

renderTitles(titles);

//nav bar
renderNavBar(titles);

renderHero(hero);

renderResume(resume);

renderServices(services);

renderGallery(gallery);

//testimonial

renderMyBlogs(blogs);

//renderContactme(titles);
