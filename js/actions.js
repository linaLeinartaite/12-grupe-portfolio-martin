"use strict";

renderTitles(titles);

//nav bar
renderNavBar(titles);
activeSection(titles);

renderResume(resume);

renderServices(services);

renderGallery(gallery);

//testimonial

//blogs

//renderContactme(titles);
