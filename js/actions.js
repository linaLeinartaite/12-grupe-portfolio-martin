"use strict";

renderTitles(titles);

//nav bar
renderNavBar(titles);

renderResume(resume);

renderServices(services);

renderGallery(gallery);

// renderTestimonials("#testimonials-block", testimonialdata);

renderMyBlogs(blogs);

//renderContactme(titles);
